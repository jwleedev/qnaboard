<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hello.qnaboard.domain.member.MemberMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="memberId">
        insert into member (member_id, name, email, password)
        values (#{memberId}, #{name}, #{email}, #{password})
    </insert>

    <select id="findById" resultType="Member">
            select member_id, name, email, password
            from member
            where member_id = #{memberId}
    </select>

    <select id="findByLoginId" resultType="Member">
        select member_id, name, email, password
        from member
        where email = #{email}
    </select>

    <select id="findAll" resultType="Member">
        select member_id, name, email, password
        from member
    </select>
</mapper>