<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hello.qnaboard.domain.comment.CommentMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="commentId">
        INSERT into comment (post_id, member_id, content)
        VALUES (#{postId}, #{memberId}, #{content})
    </insert>

    <select id="findById" resultType="Comment">
        SELECT * From comment WHERE comment_id = #{commentId}
    </select>

    <select id="findAllByPostId" resultType="CommentResponseDto">
        SELECT
        c.comment_id,
        c.post_id,
        c.member_id,
        c.content,
        m.name
        FROM comment c
        JOIN member m ON c.member_id = m.member_id
        WHERE c.post_id = #{postId}
        ORDER BY c.comment_id ASC
    </select>

    <delete id="delete">
        DELETE FROM comment
        WHERE comment_id = #{commentId}
    </delete>
</mapper>